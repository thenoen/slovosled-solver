<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
  <Appenders>
    <Console name="Console" target="SYSTEM_OUT">
      <PatternLayout pattern="%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
    </Console>
  </Appenders>

  <Loggers>
    <!-- More verbose logging for the project classes during tests -->
    <Logger name="sk.thenoen.slovosledsolver" level="DEBUG" additivity="false">
      <AppenderRef ref="Console"/>
    </Logger>

    <!-- Keep other libraries quieter to reduce test noise -->
    <Logger name="org.springframework" level="WARN"/>

    <Root level="WARN">
      <AppenderRef ref="Console"/>
    </Root>
  </Loggers>
</Configuration>
